为了实现等号操作符，软件包管理器需要保存相匹配软件包最佳版本的插槽/子插槽对。
软件包管理器可以通过在保存软件包依赖时在冒号和等号之间插入合适的插槽/子插槽对来实现此目的。
此语法只能由软件包管理器使用而不能在ebuild中使用。
这里的子插槽部分不能省略（如果SLOT变量省略了子插槽部分，那么软件包就视为具有和主插槽相等的隐式子插槽）。

每当在启用的依赖组中使用插槽等号操作符时，依赖（DEPEND）必须确保在构建时已经装好了匹配的软件包。在PDEPEND和任选依赖组中使用插槽等号操作符是无效的。
